# Backend origin the built frontend calls. Leave BLANK for local dev — Vite's
# proxy forwards /api and /actuator to http://localhost:8080 (see vite.config.js).
# In production (e.g. Vercel) set this to your deployed backend, e.g.:
#   VITE_API_BASE_URL=https://ashoo.fly.dev
VITE_API_BASE_URL=
